# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

Low-resolution License Plate Recognition (LPR) project. The repository is in early setup — no source files exist yet.

## Directory Structure

- `src/` — source code (currently empty)
- `data/` — input data such as images (currently empty)
- `reports/` — output reports and results (currently empty)
